diff --git a/apps/api/.env.example b/apps/api/.env.example
index 743b60c..de04e2f 100644
--- a/apps/api/.env.example
+++ b/apps/api/.env.example
@@ -10,9 +10,6 @@ DB_PASS=
 
 JWT_SECRET=
 
-PLATFORM_ADMIN_NAME="Administrador da Plataforma"
-PLATFORM_ADMIN_EMAIL=
-PLATFORM_ADMIN_PASSWORD=
 
 COMPANY_LEGAL_NAME=
 COMPANY_TRADE_NAME=
diff --git a/apps/api/.env.local.example b/apps/api/.env.local.example
index 1385a18..670bc4d 100644
--- a/apps/api/.env.local.example
+++ b/apps/api/.env.local.example
@@ -15,9 +15,6 @@ DB_PASS=n3xtime_root
 # Apenas desenvolvimento local. Use um segredo forte no servidor.
 JWT_SECRET=n3xtime-local-development-secret
 
-PLATFORM_ADMIN_NAME="Administrador da Plataforma"
-PLATFORM_ADMIN_EMAIL=
-PLATFORM_ADMIN_PASSWORD=
 
 COMPANY_LEGAL_NAME=
 COMPANY_TRADE_NAME=
diff --git a/apps/api/api/scripts/seedPlatformAdmin.ts b/apps/api/api/scripts/seedPlatformAdmin.ts
index 059d216..bf496ab 100644
--- a/apps/api/api/scripts/seedPlatformAdmin.ts
+++ b/apps/api/api/scripts/seedPlatformAdmin.ts
@@ -1,27 +1,136 @@
-import dotenv from 'dotenv';
+import readline from 'node:readline';
+import { createInterface } from 'node:readline/promises';
+import { stdin as input, stdout as output } from 'node:process';
 import bcrypt from 'bcrypt';
+import { sequelize } from '../config/database.js';
 import { connectDatabase } from '../models/index.js';
 import { PlatformUser } from '../models/PlatformUser.js';
 
-dotenv.config();
+const PASSWORD_POLICY_MESSAGE = 'Use de 8 a 72 caracteres, incluindo letra maiúscula, minúscula e número.';
+
+const validatePassword = (password: string) => {
+  if (
+    password.length < 8 ||
+    password.length > 72 ||
+    !/[a-z]/.test(password) ||
+    !/[A-Z]/.test(password) ||
+    !/[0-9]/.test(password)
+  ) {
+    throw new Error(PASSWORD_POLICY_MESSAGE);
+  }
+};
+
+const readHidden = (label: string): Promise<string> => {
+  if (!input.isTTY || !output.isTTY || typeof input.setRawMode !== 'function') {
+    throw new Error('Este comando precisa ser executado em um terminal interativo.');
+  }
+
+  return new Promise((resolve, reject) => {
+    let value = '';
+    const previousRawMode = input.isRaw;
+
+    const cleanup = () => {
+      input.off('keypress', onKeypress);
+      input.setRawMode(previousRawMode ?? false);
+      input.pause();
+    };
+
+    const onKeypress = (text: string, key: readline.Key) => {
+      if (key.ctrl && key.name === 'c') {
+        cleanup();
+        output.write('\n');
+        reject(new Error('Operação cancelada.'));
+        return;
+      }
+
+      if (key.name === 'return' || key.name === 'enter') {
+        cleanup();
+        output.write('\n');
+        resolve(value);
+        return;
+      }
+
+      if (key.name === 'backspace') {
+        if (value.length > 0) {
+          value = value.slice(0, -1);
+          output.write('\b \b');
+        }
+        return;
+      }
+
+      if (!key.ctrl && !key.meta && text) {
+        value += text;
+        output.write('*');
+      }
+    };
+
+    readline.emitKeypressEvents(input);
+    input.setRawMode(true);
+    input.resume();
+    input.on('keypress', onKeypress);
+    output.write(label);
+  });
+};
+
+const readIdentity = async () => {
+  const terminal = createInterface({ input, output });
+
+  try {
+    const name = (await terminal.question('Nome do administrador: ')).trim();
+    const email = (await terminal.question('E-mail: ')).trim().toLowerCase();
+
+    if (name.length < 2) {
+      throw new Error('Informe um nome válido para o administrador.');
+    }
+
+    if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
+      throw new Error('Informe um e-mail válido.');
+    }
+
+    return { name, email };
+  } finally {
+    terminal.close();
+  }
+};
 
 const run = async () => {
+  console.log('\nN3xTime — criação do administrador da plataforma\n');
+
+  const { name, email } = await readIdentity();
+  const password = await readHidden('Senha: ');
+  const passwordConfirmation = await readHidden('Confirme a senha: ');
+
+  if (password !== passwordConfirmation) {
+    throw new Error('As senhas não coincidem.');
+  }
+
+  validatePassword(password);
   await connectDatabase();
-  const name = process.env.PLATFORM_ADMIN_NAME || process.env.ADMIN_NAME || 'Administrador da Plataforma';
-  const email = (process.env.PLATFORM_ADMIN_EMAIL || process.env.ADMIN_EMAIL)?.trim().toLowerCase();
-  const password = process.env.PLATFORM_ADMIN_PASSWORD || process.env.ADMIN_PASSWORD;
-  if (!email || !password) {
-    throw new Error('Defina PLATFORM_ADMIN_EMAIL e PLATFORM_ADMIN_PASSWORD no .env');
+
+  const existingUser = await PlatformUser.findOne({ where: { email } });
+  if (existingUser) {
+    throw new Error(`Já existe um administrador da plataforma com o e-mail ${email}.`);
   }
 
-  const [user, created] = await PlatformUser.findOrCreate({
-    where: { email },
-    defaults: { name, email, password_hash: await bcrypt.hash(password, 12), status: 'active' },
+  const user = await PlatformUser.create({
+    name,
+    email,
+    password_hash: await bcrypt.hash(password, 12),
+    status: 'active',
   });
-  console.log(created ? `Administrador global ${user.email} criado.` : `Administrador global ${user.email} já existe.`);
+
+  console.log(`\nAdministrador da plataforma ${user.email} criado com sucesso.`);
 };
 
-run().catch((error) => {
-  console.error(error);
-  process.exit(1);
-});
+run()
+  .catch((error) => {
+    console.error(`\nErro: ${error instanceof Error ? error.message : String(error)}`);
+    process.exitCode = 1;
+  })
+  .finally(async () => {
+    try {
+      await sequelize.close();
+    } catch {
+      // A conexão pode não ter sido aberta quando a validação falhar antes do acesso ao banco.
+    }
+  });
diff --git a/apps/api/package.json b/apps/api/package.json
index 90ab2a9..044931f 100644
--- a/apps/api/package.json
+++ b/apps/api/package.json
@@ -12,7 +12,9 @@
     "start:prod": "node dist/api/server.js",
     "start:pm2": "node dist/api/server.js",
     "seed:admin": "tsx api/scripts/seedAdmin.ts",
-    "seed:platform-admin": "tsx api/scripts/seedPlatformAdmin.ts"
+    "seed:platform-admin": "tsx api/scripts/seedPlatformAdmin.ts",
+    "admin:create": "tsx api/scripts/seedPlatformAdmin.ts",
+    "admin:create:prod": "node dist/api/scripts/seedPlatformAdmin.js"
   },
   "dependencies": {
     "@types/nodemailer": "^8.0.1",
diff --git a/package.json b/package.json
index dd55ae6..4c449ec 100644
--- a/package.json
+++ b/package.json
@@ -12,6 +12,8 @@
     "build:web": "pnpm --filter @n3xtime/web build",
     "lint": "pnpm --filter @n3xtime/web lint",
     "typecheck": "pnpm --filter @n3xtime/web typecheck",
+    "admin:create": "pnpm --filter @n3xtime/api admin:create",
+    "admin:create:prod": "pnpm --filter @n3xtime/api admin:create:prod",
     "db:local:up": "docker compose -f docker-compose.local.yml up -d --wait mysql",
     "db:local:stop": "docker compose -f docker-compose.local.yml stop mysql",
     "db:local:down": "docker compose -f docker-compose.local.yml down",
